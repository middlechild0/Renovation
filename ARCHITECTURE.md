# Renovation Client Portal - System Architecture

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    CLIENT BROWSER                            │
│                  http://localhost:3000                       │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│              NEXT.JS FRONTEND (Port 3000)                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Components:                                         │   │
│  │  • DemoSearch.tsx    - Search interface             │   │
│  │  • DemoCard.tsx      - Demo display cards           │   │
│  │  • PaymentModal.tsx  - Stripe payment UI            │   │
│  │  • Header.tsx        - Navigation                   │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Pages:                                              │   │
│  │  • /                 - Home (search demos)          │   │
│  │  • /my-demos         - View purchased demos         │   │
│  └─────────────────────────────────────────────────────┘   │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        │ API Proxy (next.config.js)
                        │ /api/* → http://localhost:5000/api/*
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│              FLASK BACKEND (Port 5000)                       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  REST API Endpoints:                                 │   │
│  │  POST /api/demos/search      - Search demos         │   │
│  │  POST /api/payments/create   - Create payment       │   │
│  │  POST /api/payments/confirm  - Confirm payment      │   │
│  │  POST /api/client/demos      - Get client's demos   │   │
│  │  POST /api/demo/access/:id   - Verify access        │   │
│  │  GET  /api/health            - Health check         │   │
│  └─────────────────────────────────────────────────────┘   │
└─────┬───────────────────────┬───────────────────────────┬───┘
      │                       │                           │
      ▼                       ▼                           ▼
┌─────────────┐    ┌──────────────────┐    ┌────────────────────┐
│  SQLite DB  │    │  Stripe API      │    │ demo_results.json  │
│             │    │                  │    │                    │
│ client_     │    │ Payment          │    │ • Java House       │
│ orders      │    │ Processing       │    │ • Nairobi Dental   │
│             │    │                  │    │ • River Tech       │
└─────────────┘    └──────────────────┘    └────────────────────┘
                           │
                           ▼
                   ┌──────────────────┐
                   │   Vercel URLs    │
                   │                  │
                   │ demosites-rho    │
                   │ .vercel.app      │
                   └──────────────────┘
```

## 🔄 User Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    USER JOURNEY                              │
└─────────────────────────────────────────────────────────────┘

1. DISCOVER
   User visits homepage
          │
          ▼
   Searches for "Java House"
          │
          ▼
   Frontend → POST /api/demos/search → Backend
          │                                  │
          │                                  ▼
          │                          Read demo_results.json
          │                                  │
          ◄────────────────────────────────────
                  Returns demo list

2. SELECT
   User clicks "Get Access" on demo card
          │
          ▼
   PaymentModal opens
          │
          ▼
   Frontend → POST /api/payments/create → Backend
          │                                    │
          │                                    ▼
          │                            Create Stripe Intent
          │                                    │
          ◄──────────────────────────────────────
              Returns client_secret

3. PURCHASE
   User enters email & card details
          │
          ▼
   Stripe.confirmPayment()
          │
          ▼
   Payment processed by Stripe
          │
          ▼
   Frontend → POST /api/payments/confirm → Backend
          │                                      │
          │                                      ▼
          │                              Save to database
          │                                      │
          ◄────────────────────────────────────────
              Returns demo URL

4. ACCESS
   User receives demo URL
          │
          ▼
   Can revisit /my-demos anytime
          │
          ▼
   Enter email → Fetch purchased demos
          │
          ▼
   View all demos with direct links
```

## 📦 Data Flow

```
┌──────────────────────────────────────────────────────────────┐
│                   DEMO SEARCH FLOW                            │
└──────────────────────────────────────────────────────────────┘

User types "dental"
      │
      ▼
┌──────────────────┐
│  DemoSearch.tsx  │
│  query: "dental" │
└────────┬─────────┘
         │
         │ axios.post('/api/demos/search', {query})
         ▼
┌──────────────────┐
│   Flask API      │
│   app.py         │
└────────┬─────────┘
         │
         │ load_demo_results()
         ▼
┌──────────────────────┐
│ demo_results.json    │
│ {                    │
│   "nairobi-dental":{ │
│     business_name,   │
│     industry,        │
│     demo_url         │
│   }                  │
│ }                    │
└────────┬─────────────┘
         │
         │ Filter by query
         ▼
┌──────────────────┐
│  JSON Response   │
│  [               │
│    {             │
│      id,         │
│      name,       │
│      url         │
│    }             │
│  ]               │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  DemoCard.tsx    │
│  Renders results │
└──────────────────┘


┌──────────────────────────────────────────────────────────────┐
│                   PAYMENT FLOW                                │
└──────────────────────────────────────────────────────────────┘

User clicks "Get Access"
      │
      ▼
┌──────────────────────┐
│  PaymentModal.tsx    │
│  Opens with demo ID  │
└──────────┬───────────┘
           │
           │ POST /api/payments/create
           ▼
┌──────────────────────┐
│   Flask API          │
│   Create payment     │
└──────────┬───────────┘
           │
           │ stripe.PaymentIntent.create()
           ▼
┌──────────────────────┐
│   Stripe API         │
│   Returns secret     │
└──────────┬───────────┘
           │
           │ client_secret
           ▼
┌──────────────────────┐
│  Stripe Elements     │
│  Collect card info   │
└──────────┬───────────┘
           │
           │ User submits payment
           ▼
┌──────────────────────┐
│  Stripe confirms     │
│  Payment succeeded   │
└──────────┬───────────┘
           │
           │ POST /api/payments/confirm
           ▼
┌──────────────────────┐
│   Save to DB         │
│   Grant access       │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│  Show demo URL       │
│  Payment complete!   │
└──────────────────────┘
```

## 🗄️ Database Schema

```
┌──────────────────────────────────────────────────────────────┐
│                 client_orders TABLE                           │
├──────────────────────────────────────────────────────────────┤
│ id                          INTEGER PRIMARY KEY              │
│ client_email                TEXT NOT NULL                    │
│ demo_id                     TEXT NOT NULL                    │
│ business_name               TEXT NOT NULL                    │
│ payment_status              TEXT NOT NULL                    │
│ demo_url                    TEXT NOT NULL                    │
│ price_paid                  DECIMAL(10, 2)                   │
│ created_at                  TIMESTAMP                        │
│ access_granted              BOOLEAN DEFAULT 0                │
│ stripe_payment_intent_id    TEXT                             │
└──────────────────────────────────────────────────────────────┘

Example Row:
┌────┬─────────────────────┬──────────────┬──────────────┬──────────┬─────────────┬───────┬────────────┬────────┬──────────┐
│ id │ client_email        │ demo_id      │ business_name│ payment  │ demo_url    │ price │ created_at │ access │ stripe_id│
├────┼─────────────────────┼──────────────┼──────────────┼──────────┼─────────────┼───────┼────────────┼────────┼──────────┤
│ 1  │ john@example.com    │ java-house-1 │ Java House   │completed │https://...  │ 50.00 │ 2024-01-20 │ 1      │ pi_xxx   │
└────┴─────────────────────┴──────────────┴──────────────┴──────────┴─────────────┴───────┴────────────┴────────┴──────────┘
```

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                   SECURITY ARCHITECTURE                      │
└─────────────────────────────────────────────────────────────┘

1. Environment Variables
   ┌────────────────────┐
   │  .env files        │
   │  • Never committed │
   │  • API keys hidden │
   └────────────────────┘

2. Stripe PCI Compliance
   ┌────────────────────┐
   │  Stripe Elements   │
   │  • No card storage │
   │  • Tokenization    │
   └────────────────────┘

3. CORS Protection
   ┌────────────────────┐
   │  Flask-CORS        │
   │  • Origin control  │
   │  • Method control  │
   └────────────────────┘

4. Payment Verification
   ┌────────────────────┐
   │  Backend checks    │
   │  • Intent verify   │
   │  • Access control  │
   └────────────────────┘
```

## 📱 Component Hierarchy

```
App Layout (layout.tsx)
│
├─ Header
│  ├─ Logo
│  └─ Navigation
│     ├─ Browse Demos (/)
│     ├─ My Demos (/my-demos)
│     └─ Support (email)
│
├─ Home Page (/)
│  ├─ Hero Section
│  └─ DemoSearch
│     ├─ Search Input
│     ├─ Search Button
│     └─ DemoCard (multiple)
│        ├─ Demo Preview
│        ├─ Demo Details
│        ├─ Price
│        ├─ Get Access Button
│        └─ PaymentModal (conditional)
│           ├─ Email Input
│           ├─ Stripe Elements
│           │  └─ Payment Element
│           ├─ Submit Button
│           └─ Success State
│
└─ My Demos Page (/my-demos)
   ├─ Email Input
   ├─ Fetch Button
   └─ Demo List
      └─ Demo Item (multiple)
         ├─ Business Name
         ├─ Purchase Date
         ├─ Demo URL
         └─ View Button
```

## 🌐 API Endpoints Map

```
Backend API (Flask)
http://localhost:5000

├─ /api/demos/search
│  Method: POST
│  Body: {query: string}
│  Returns: Array<Demo>
│
├─ /api/payments/create
│  Method: POST
│  Body: {demo_id, client_email}
│  Returns: {client_secret, demo_url}
│
├─ /api/payments/confirm
│  Method: POST
│  Body: {demo_id, client_email, payment_intent_id}
│  Returns: {success, access_url, message}
│
├─ /api/client/demos
│  Method: POST
│  Body: {client_email}
│  Returns: Array<PurchasedDemo>
│
├─ /api/demo/access/:demo_id
│  Method: POST
│  Body: {client_email}
│  Returns: {access_url, expires_at}
│
└─ /api/health
   Method: GET
   Returns: {status, service, version}
```

## 🎯 Integration Points

```
┌─────────────────────────────────────────────────────────────┐
│         RENOVATION AUTOMATIONS ECOSYSTEM                     │
└─────────────────────────────────────────────────────────────┘

Existing System                    New Portal
───────────────                    ──────────

┌──────────────────┐              ┌──────────────────┐
│ 4-Agent Framework│              │   Next.js App    │
│ • Tier Analyzer  │              │   • Search UI    │
│ • Competitive    │              │   • Payments     │
│ • Synthesizer    │              │   • Access       │
│ • Composer       │              └────────┬─────────┘
└────────┬─────────┘                       │
         │                                 │
         │ Generates                       │ Displays
         ▼                                 │
┌──────────────────┐                       │
│demo_results.json │◄──────────────────────┘
│ • Java House     │       Reads
│ • Nairobi Dental │
│ • River Tech     │
└────────┬─────────┘
         │
         │ Deployed to
         ▼
┌──────────────────┐
│  Vercel Hosting  │
│ demosites-rho    │
│ .vercel.app      │
└──────────────────┘
         ▲
         │
         │ Links to
         │
┌──────────────────┐
│   Client Portal  │
│   (after payment)│
└──────────────────┘
```

This architecture enables:
✅ Separation of concerns (generation vs. serving)
✅ Scalable payment processing
✅ Existing demo reuse (no rebuild needed)
✅ Clean API design
✅ Future extensibility
